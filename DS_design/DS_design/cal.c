#include "list.h"

int main()
{
	//SList a, b;
	/*int a, b;
	char buf[1024];
	scanf("%s", buf);
	int sz = strlen(buf);
	while (buf)
	{
		
	}*/


	/*SListInit(&a);
	SListInit(&b);

	ElemType item, index;


	printf("请输入要插入的值[以-1结束]:>");
	while (scanf("%d %d", &item, &index), item != -1)
	{
		SListPushBack(&a, item, index);
	}
	printf("尾部插入数据成功...\n");
	
	printf("a = ");
	SListShow(&a);
			
	printf("请输入要插入的值[以-1结束]:>");
	while (scanf("%d %d", &item, &index), item != -1)
	{
		SListPushBack(&b, item, index);
	}
	printf("尾部插入数据成功...\n");

	printf("b = ");
	SListShow(&b);

	Sublist(&a, &b);
	printf("a = ");
	SListShow(&a);
	SListDestroy(&a);*/


	printf("GoodBye......\n");

	system("pause");
	return 0;
}